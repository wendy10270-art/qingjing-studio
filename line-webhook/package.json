{
  "name": "qingjing-line-webhook",
  "version": "1.0.0",
  "private": true,
  "description": "LINE Messaging API webhook — binds students' LINE userId to their phone number for class reminders",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "firebase-admin": "^14.0.0",
    "nodemailer": "^6.9.0"
  }
}
